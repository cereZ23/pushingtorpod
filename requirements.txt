# FastAPI and web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0  # Required for Pydantic email validation

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
asyncpg==0.29.0
alembic==1.13.1

# Celery and Redis
celery==5.3.4
croniter>=1.4.1  # Cron expression evaluation for scheduled scans
redis==5.0.1

# MinIO/S3
minio==7.2.3

# JWT and security
pyjwt==2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.1.2  # Pin bcrypt 4.x for passlib compatibility
python-multipart>=0.0.21
slowapi==0.1.9  # Rate limiting for FastAPI
python3-saml>=1.16.0  # SAML2 SSO authentication
pyotp>=2.9.0  # TOTP MFA
qrcode[pil]>=7.4  # QR code generation for MFA setup

# HTTP client
httpx==0.26.0
requests>=2.33.0

# YAML for configuration
pyyaml==6.0.1

# Testing
pytest>=8.3.0,<9.0.0  # pytest 8.3+ fixes tmpdir CVE; 9.x breaks pytest-asyncio
pytest-asyncio>=0.24.0
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-xdist==3.5.0
pytest-timeout==2.2.0
pytest-benchmark==4.0.0
factory-boy==3.3.0
faker==22.0.0
psutil==5.9.7
coverage[toml]==7.4.0

# Utilities
python-dateutil==2.8.2
tldextract==5.1.1  # Domain validation (Sprint 2 security fix)
cryptography>=44.0.3  # Secret encryption — bumped for DNS constraint enforcement fix
python-whois==0.9.4  # WHOIS/RDAP lookups for hidden asset discovery
geoip2>=4.8.0  # MaxMind GeoLite2 local database lookups (City + ASN)
dnstwist==20240116  # Domain permutation / typosquatting detection

# Visual Recon
# Pin to 1.56.0 (last release before the Chrome for Testing switch): v1.57
# dropped open-source Chromium and switched to Chrome for Testing which
# consumes up to ~20 GB RAM per instance (see playwright issue #38489).
# We observed the worker OOM-killed at 12.4 GB RSS on every tier 3 scan
# after bumping to 1.58.0. Downgrading restores the Chromium build and
# drops the visual-recon phase peak to ~1 GB.
playwright==1.56.0
Pillow>=10.0.0

# Report generation (PDF, DOCX, charts)
weasyprint>=62.0
python-docx>=1.1.0
matplotlib>=3.9.0
Jinja2>=3.1.0

# Monitoring and Error Tracking
prometheus-client>=0.21.0
sentry-sdk[celery,sqlalchemy]==2.19.2
